import React from "react";
import ReactDOM from "react-dom/client";
import RfePage from "./rfeApp";

ReactDOM.createRoot(document.getElementById("rfePage")).render(
  <React.StrictMode>
    <RfePage />
  </React.StrictMode>
);
